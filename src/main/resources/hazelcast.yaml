hazelcast:
  network:
    join:
      multicast:
        enable: true
  map:
    default:
      time-to-live-seconds: 10
      max-idle-seconds: 10
      eviction:
        eviction-policy: LRU
        max-size-policy: PER_NODE
        size: 500